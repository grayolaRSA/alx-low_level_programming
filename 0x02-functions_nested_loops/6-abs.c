#include "main.h"
#include <stdio.h>

/**
 *_abs - calculates the absolute value of an integer
 * @n: describe
 *
 *Return: always 0
 */


int _abs(int n)

{

	if (n >= 0)

	{
		return (n);
	}

	else
	{
		return (-n);
	}
	return (0);
}
